@model IEnumerable<MSE.SWE.Interfaces.IUser>

@{
    ViewBag.Title = "User Administration";
}

<h2>User Administration</h2>

<p>
    @Html.ActionLink("Create New", "Create") | @Html.ActionLink("Home", "Index", "Home")
</p>
<table class="table">
    <tr>
        <th>Email</th>
        <th>Group</th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>@item.EMail</td>
        <td>@item.Group</td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
            @Html.ActionLink("Details", "Details", new { id = item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ID })
        </td>
    </tr>
}

</table>
<p>@Html.ActionLink("Previous", "Index", new { id = (ViewBag.AdminUserControllerPage - 1) }) | 
    @Html.ActionLink("Next", "Index", new { id = (ViewBag.AdminUserControllerPage + 1) })</p>